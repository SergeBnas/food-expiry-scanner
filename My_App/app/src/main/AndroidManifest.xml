<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.FoodExpiryScanner"
        android:usesCleartextTraffic="true">

        <activity android:name=".ui.library.LibraryActivity" />
        <activity android:name=".ui.meals.MealSuggestionsActivity" />
        <activity android:name=".ui.insights.InsightsActivity" />
        <activity android:name=".ui.expiring.ExpiringSoonActivity" />
        <activity android:name=".ui.addedit.AddEditItemActivity" />
        <activity
            android:name=".ui.scan.PortraitCaptureActivity"
            android:theme="@style/Theme.FoodExpiryScanner" />
        <activity android:name=".ui.scan.ScanActivity" />
        <activity
            android:name=".ui.home.MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
